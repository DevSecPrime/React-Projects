const JSXRules = () => {
  return (
    <>
      <h1>JSX Rules</h1>
      <p>JSX must return a single parent element.</p>
      <p>JSX must be properly closed</p>
      <p>
        JSX attributes are written in camelCase (e.g. className instead of
        class)
      </p>
    </>
  );
};

export default JSXRules;
